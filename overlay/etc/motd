Samsung NVMe firmware environment

The internal NVMe SSD is not mounted. Run:
    fumagician

The wrapper refuses to start if an NVMe filesystem is mounted or if the
configured target drive model is not detected, and it verifies the
firmware revision after flashing instead of exiting silently.
